# 1. Dependencies (Most Important)
# This folder is huge and can be re-installed by anyone using 'npm install'
node_modules/

# 2. Secrets (Critical for Security)
# Never, ever commit your environment variables and secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 3. Logs
# These files change constantly and are specific to your machine
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 4. OS & System Files
# Files generated by your operating system
.DS_Store
Thumbs.db

# 5. IDE & Editor Folders
# Settings specific to your code editor (e.g., VS Code, WebStorm)
.vscode/
.idea/

# 6. Build Output
# If you ever 'build' your project, you don't need to commit the output
dist/
build/